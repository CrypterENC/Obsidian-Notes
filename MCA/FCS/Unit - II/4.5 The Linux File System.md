
- Most of the Linux **~={green}System entities is treated as a file=~**

### Administrator Choice :
- Select f**~={green}ile system best suited=~** for OS and files

**Common Linux File Systems:**

- ext2, ext3, ext4
- NFS (Network File System)
- CDFS (CD File System)

#### Mounting
-  Process of **~={green}assigning directory to partition.=~**
- After mount, file system on partition accessible through directory
- Directory = mounting point

### Linux Roles and File permissions 

-  Linux **~={yellow}uses file permissions=~** in ~={green}**order to organize the system and enforce boundaries**=~ within the computer
- Every file in Linux carries its own  **~={green}file permissions,=~** which ~={yellow}define that actions that the owner, the group and others=~ can ~={green}**perform with the file**=~

- Use this command `ls -l` which lists the additional information about the file

`ls -l space.txt`
**Example: `-rwxrw-r-- 1 idor staff 256`**

**Permission Breakdown:**

- **First character:** `-` = file, `d` = directory
- **Characters 2-4 (User):** `rwx` = owner can read, write, execute
- **Characters 5-7 (Group):** `rw-` = group can read, write (not execute)
- **Characters 8-10 (Other):** `r--` = others can only read

### **File Information Fields**

**1. Permissions** - User, Group, Other rights

**2. Hard Links Count** - Number of hard links to file

**3. Owner** - User who owns file

**4. Group** - Group that owns file

**5. File Size** - In bytes

**6. Modification Date/Time** - Last change timestamp

**7. File Name** - Name of file
